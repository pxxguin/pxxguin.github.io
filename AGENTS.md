# Repository Instructions

## Git 작성자 및 기여자 정보

- 커밋하거나 푸시할 때 Codex, OpenAI, AI assistant 또는 에이전트의 이름과 이메일을 Author, Committer, Contributor로 기록하지 않는다.
- 커밋 메시지에 `Co-authored-by`, `Signed-off-by`, `Assisted-by`, 생성 도구 표기 등 에이전트를 기여자로 나타내는 트레일러나 문구를 추가하지 않는다.
- 저장소 또는 전역 Git의 `user.name`, `user.email`을 에이전트 정보로 설정하거나 변경하지 않는다.
- 커밋을 요청받으면 현재 사용자의 기존 Git identity를 그대로 사용한다. 사용자 identity가 없어서 커밋할 수 없다면 임의의 identity를 만들지 말고 사용자에게 알린다.
- 푸시할 때 커밋 작성자 정보를 다시 쓰거나 에이전트 관련 메타데이터를 추가하지 않는다.

## 블로그 포스팅 작성 원칙

`src/content/posts/` 아래에 새 포스팅을 작성하거나 기존 포스팅의 본문을 크게 수정할 때는 아래 문체와 구성을 기본값으로 사용한다. 단순 교정이나 코드 수정에서는 불필요하게 본문 전체를 재작성하지 않는다.

### AI 에이전트 엔지니어링 시리즈

- 「AI 에이전트 엔지니어링」 PDF를 바탕으로 포스팅을 작성할 때는 작업 전에 `.agents/ai-agent-engineering-series.md`를 읽는다.
- 해당 문서에 정한 총 24편의 순서, 책/PDF 페이지 범위, 포스팅 사이의 내용 경계를 따른다.
- 특정 포스팅을 요청받으면 문서에 배정된 페이지와 앞뒤 1쪽을 확인하되, 앞뒤 포스팅의 담당 내용을 중복해 상세히 작성하지 않는다.

### 기준으로 삼을 글

- **문체의 유일한 기준은 frontmatter의 `postId`가 1부터 26까지인 포스팅이다.** 27번 이후 포스팅의 문장, 구성, 표현, 어조를 문체 학습이나 모방의 근거로 사용하지 않는다.
- 1번부터 26번까지의 글 중에서도 본문이 충분히 작성된 완성 글을 기준으로 삼는다. `draft: true`인 글, 본문이 비어 있는 글, 제목이나 소제목만 있고 내용이 명백히 미완성인 글은 문체 표본에서 제외한다.
- 같은 주제의 글이 있다면 해당 시리즈를 먼저 참고한다. 전체적인 목소리는 DeepL·Docker 글의 친근한 질문과 쉬운 비유, Terraform 글의 단계별 실습, GitHub 23~26번 글의 구체적인 상황 설정과 보안 문제 전개를 함께 기준으로 삼는다.
- 1~26번 글과 이 문서의 일반적인 글쓰기 조언이 충돌하면 **1~26번 원문의 실제 문체를 우선한다.** 작성자의 표현이 일반적인 기술 문서 문체와 다르거나 다소 거칠게 느껴지더라도, 에이전트의 취향을 이유로 말투를 고치거나 평준화하지 않는다.
- 오탈자, 잘못된 코드 fence, 실행되지 않는 코드, 부정확한 사실은 문체와 구분해서 바로잡을 수 있다. 이때도 오류가 있는 부분만 최소한으로 수정하고, 문장의 어미·호흡·반복·질문 방식·감정 표현까지 함께 교정하지 않는다.
- 새 글을 쓰기 전에는 1~26번 중 주제와 형식이 가장 가까운 완성 글을 최소 2편 직접 읽는다. 시리즈 글이라면 1~26번 범위에 있는 같은 시리즈 글을 우선한다.

### 글의 목소리

- 독자에게 옆에서 설명하듯 친근한 존댓말을 기본으로 사용한다. 다만 설명과 결론에서는 1~26번 글처럼 짧은 선언형 문장을 섞어 문장에 힘을 줄 수 있다.
- 개념을 바로 정의하기보다 먼저 문제나 의문을 던진다. “왜 사용하는걸까요?”, “그러면 어떻게 해결해야할까요?”, “진짜 된다고요?”처럼 독자가 실제로 물을 법한 질문에서 설명을 시작한다.
- “알아보겠습니다”, “직접 테스트를 해봅시다”, “생각하시면 편합니다”, “결과를 보면”처럼 독자와 함께 확인하는 표현을 자연스럽게 사용한다.
- 지나치게 건조한 교과서 문체나 홍보 문구는 피한다.
- 가벼운 농담, 괄호, 말줄임표, `(?)` 등은 흐름을 풀어줄 때만 제한적으로 사용한다. “딸.깍”, “반.드.시”, “아무런(?) 지장”처럼 원문에 실제로 나타난 장난스러운 호흡은 강조가 필요한 지점에서만 사용한다.
- 어려운 용어는 처음 등장할 때 원어·약어와 함께 풀고, 바로 쉬운 표현으로 다시 설명한다.

### 작성자의 사고 과정과 문장 리듬

- 친근하게 정리된 기술 문서보다, 작성자가 실제로 공부하면서 가졌던 의문과 이해 과정을 보여준다. “여기서 제가 공부할 때 궁금했던 게”, “이해합니다. 이게 뭔소리인지 모르겠죠”, “제가 실제로 해킹할 수 없으니”처럼 설명자의 위치를 숨기지 않는다.
- 독자가 가질 법한 반론을 먼저 꺼낸다. “그냥 한 번에 연산하면 편할 텐데”, “git clone을 쓰면 되지 왜 굳이 사용하나요?”, “Dockerfile이 있는데 docker-compose도 적어야 할까요?”처럼 상식적인 의문에 답하며 전개한다.
- 공식 정의를 그대로 나열하기보다 도서관에서 책 찾기, 호텔 카운터, 팀의 PR 검토, 사용자가 서버에 요청을 보내는 과정처럼 1~26번 글에서 사용한 현실적인 장면으로 다시 풀어쓴다.
- “예.. 복잡해집니다”, “쉽죠?”, “말이죠;;;”, “믿을 만하겠죠(?)”처럼 생각이 멈추거나 전환되는 짧은 호흡을 사용할 수 있다. 문단마다 반복하거나 실제 원문보다 과장하지 않는다.
- 물음표·느낌표·말줄임표와 괄호 속 반응은 독자가 의문을 느껴야 하는 지점이나 예상 밖의 결과에만 사용한다. 문체를 흉내 내기 위해 감탄사를 임의로 추가하지 않는다.
- 기술 개념을 사람이나 익숙한 물건처럼 표현할 수 있다. 단, 비유 뒤에는 실제 기술적 의미를 곧바로 연결한다.
- 문장 전체의 온도를 균일하게 만들지 않는다. 정확한 기술 설명 사이에 짧은 질문, 감상, 놀람, 개인적인 판단을 섞어 작성자가 직접 말하는 느낌을 유지한다.
- 강조는 정의와 결론에만 사용하지 않는다. 독자가 문제를 발견해야 하는 사건, 핵심 질문, 예상 밖의 결과에도 목적에 맞는 강조 문법을 사용한다.
- 일반적인 사실, 실습에서 확인한 결과, 작성자의 판단을 구분한다. “일반적으로”, “결과를 보면”, “저는/제가 생각하기에는” 같은 표현으로 근거의 성격을 드러낸다.
- 작성자의 개성을 살리되 `합니당` 같은 단발성 오타나 문법 오류를 고정된 말투로 모방하지 않는다. 사실관계와 가독성은 유지한다.

### 작성자의 말투 보존

- **작성자의 말투는 교정 대상이 아니다.** 에이전트가 더 자연스럽다거나 전문적으로 보인다고 판단한 표현으로 임의 교체하지 않는다.
- 기존 본문을 수정할 때는 요청받은 문제만 고친다. 주변 문장의 조사, 어미, 문장 길이, 반복, 감탄사, 말줄임표, 괄호 속 반응, 질문형 전개는 요청이 없는 한 그대로 둔다.
- 새 글을 작성할 때도 일반적인 기술 블로그 문체로 매끈하게 평준화하지 않는다. 1~26번에서 확인한 작성자의 표현 선택과 문장 호흡을 우선한다.
- “더 깔끔하다”, “더 전문적이다”, “문법적으로 더 자연스럽다”, “AI가 보기에 가독성이 좋다”는 이유만으로 문체를 바꾸지 않는다.
- 문체를 크게 바꿔야 해결되는 요청이거나 작성자의 표현과 정확성 사이에 충돌이 생기면 임의로 결정하지 말고 사용자에게 먼저 알린다.
- 교정이나 퇴고를 명시적으로 요청받더라도 사실 오류, 명백한 오탈자, 문법 오류와 작성자의 고유한 말투를 구분한다. 수정 후에도 사용자가 직접 쓴 글처럼 읽혀야 한다.

### 1~26번에서 확인한 문체의 핵심

- 사용자의 글을 단순히 “친근한 존댓말”로 해석하지 않는다. ==작성자가 실제로 궁금해하고, 직접 확인하고, 판단하는 과정을 독자에게 보여주는 글==이 핵심이다.
- 정의부터 시작하는 사전식 구성을 피한다. 독자가 겪을 법한 상황을 먼저 만들고, “왜 사용하는걸까요?”, “그냥 기존 방식으로 하면 안 될까요?”, “그러면 어떻게 될까요?”와 같은 질문을 거쳐 개념을 등장시킨다.
- 질문은 문장을 꾸미기 위해 넣지 않는다. 다음 설명이 왜 필요한지를 만드는 연결 장치로 사용한다. 필요성에 대한 질문, 독자의 반론, 결과를 예측하는 질문, 결과 이후의 현실적인 판단이 자연스럽게 이어지도록 한다.
- 작성자의 존재를 숨기지 않는다. “제가 공부할 때 궁금했던 게”, “저는 이렇게 생각했습니다”, “제가 실제로 확인해보면”처럼 공부하면서 가졌던 의문, 실습에서 확인한 결과, 개인적인 선택을 직접 드러낼 수 있다.
- 독자를 강의 대상처럼 멀리 두지 않는다. “우리가”, “한번 볼까요?”, “직접 테스트를 해봅시다”, “생각하시면 편합니다”처럼 작성자와 독자가 함께 확인하는 흐름을 만든다.
- 긴 기술 설명 다음에 “예.. 그렇습니다”, “그래서 씁니다”, “쉽죠?”, “귀찮겠죠. 당연히.”와 같은 짧은 반응을 섞는다. 모든 문장을 비슷한 길이와 온도로 정돈하지 않는다.
- “일반적으로”, “이 부분”, “우리가”, “실제로”, “그렇다면”, “이때”와 같은 표현이 반복되더라도 에이전트의 취향으로 유의어를 찾아 바꾸지 않는다. 반복도 작성자의 문장 호흡에 포함될 수 있다.
- 말줄임표, 괄호 속 반응, 질문의 반복, “반.드.시”, “무 조 건”, “딸.깍”, “사망(?)”과 같은 표현을 유치하거나 불필요하다는 이유로 삭제하지 않는다. 새 글에서는 실제 문맥상 자연스러운 경우에만 사용하고 억지로 복제하지 않는다.

### 문제와 설명을 전개하는 방식

- 글의 기본 흐름은 “현실적인 문제 상황 → 기존 방식에 대한 반론 → 부족한 이유 → 핵심 개념 → 직접 확인 → 결과 해석 → 남은 문제와 작성자의 판단”으로 잡는다. 주제에 맞지 않으면 일부 단계는 생략할 수 있지만, 목차처럼 기계적으로 채우지는 않는다.
- 내용을 먼저 “세 가지 질문”, “네 가지 범주”, “다섯 단계”처럼 균일한 개수로 분류한 뒤 하나씩 채우지 않는다. 실제 내용상 숫자가 중요한 경우가 아니라면 작성자가 궁금해한 순서대로 설명한다.
- 표를 제거한 뒤 같은 내용을 번호 목록으로 옮기는 방식도 피한다. 표와 목록의 모양만 다를 뿐, 모든 항목을 같은 분량과 같은 구조로 설명하면 여전히 AI가 정리한 글처럼 보일 수 있다.
- 여러 사례가 있더라도 모두 동등한 비중으로 반듯하게 배치하지 않는다. 가장 이해하기 쉬운 사례 하나를 중심으로 끝까지 설명하고, 나머지 사례는 “이 경우도 마찬가지겠죠?”처럼 필요한 지점에 짧게 연결한다.
- 비슷한 개념을 설명할 때 각 항목마다 “정의 → 허용 이유 → 예외 → 핵심 요약”을 반복하지 않는다. 첫 번째 개념을 설명하면서 생긴 질문이나 한계가 다음 개념을 등장시키도록 연결한다.
- 두 개념을 비교할 때 장점과 단점을 정확히 대칭으로 맞추지 않는다. 먼저 독자가 실제로 선택해야 하는 상황을 보여주고, 왜 한쪽을 선택하게 되는지 질문과 판단을 따라 설명한다.
- “설명 → 사례 → 반례 → 핵심 한 줄” 구조를 모든 소제목에서 반복하지 않는다. 어떤 부분은 사례에서 바로 시작하고, 어떤 부분은 실행 결과나 작성자의 오해에서 시작하는 등 글의 호흡을 일부러 획일화하지 않는다.
- 긴 설명을 매번 완성된 슬로건이나 교훈 한 문장으로 압축하지 않는다. 결론이 필요하다면 “제가 이해한 방식은”, “그러니깐”, “결과를 보면”처럼 앞의 사고 과정에서 자연스럽게 나온 판단으로 적는다.
- 공식 문서의 분류 체계를 전달해야 하는 경우에도 분류표를 그대로 번역해서 나열하지 않는다. 작성자가 처음에 무엇을 위험하다고 생각했고, 다음 내용을 보면서 판단이 어떻게 넓어졌는지를 함께 보여준다.
- 추상적인 개념은 현실적인 장면으로 바꿔 설명한다. 1~26번의 도서관에서 책 찾기, 호텔 카운터, 사과 가격과 소비세, 팀의 PR 검토, Supplier·Victim·Hacker처럼 각 기술 요소가 어떤 대상에 대응되는지 알 수 있는 상황을 만든다.
- 비유만 남기지 않는다. 비유로 감을 잡은 뒤 실제 기술 요소와 동작으로 돌아와 무엇이 서로 대응되는지 설명한다.
- 하나의 실습에서는 가능하면 동일한 상황과 등장 대상을 끝까지 유지한다. 설명할 때마다 서로 관련 없는 예시를 새로 만들지 않는다.
- 기술을 무조건 추천하지 않는다. “더 좋은 도구니까 사용한다”로 끝내지 말고, 기존 방식으로 충분한 상황, 사용하지 않아도 되는 조건, 도입하면 생기는 비용과 새로운 문제를 함께 설명한다.
- 완벽한 해결책이 없는 문제는 억지로 정답을 만들지 않는다. 현재 가능한 최선책과 한계를 구분하고, 마지막에는 작성자가 중요하다고 본 질문이나 판단을 남길 수 있다.

### 실습 글의 문체와 결과 해석

- 코드를 먼저 던지지 않는다. 왜 이 실습을 하는지, 어떤 상황을 재현하는지, 필요한 역할이나 디렉터리가 무엇인지 설명한 뒤 코드를 보여준다.
- 명령이나 코드를 보여준 다음에는 “결과를 보면”과 같이 실제 출력에서 독자가 확인해야 하는 부분을 짚는다. 코드와 출력만 나열하고 다음 단계로 넘어가지 않는다.
- 성공하는 흐름만 보여주지 않는다. 주제의 핵심을 이해하는 데 필요하다면 잘못된 설정, 실패한 테스트, 오래된 실행 계획, 권한 문제, 공격이 성립하는 순간도 직접 재현하고 그 결과를 해석한다.
- 보안 글에서는 공격 코드를 자극적으로 나열하는 데서 끝내지 않는다. 공격에 필요한 전제, 실제로 위험해지는 지점, 방어가 어려운 이유, 현실적인 최선책을 차례로 설명한다.

### 마무리 방식

- 모든 글을 동일한 요약 템플릿이나 정형화된 체크리스트로 끝내지 않는다. 이는 1~26번의 문체보다 AI가 정리한 글처럼 보일 수 있다.
- 마무리는 이번 실습에서 확인한 핵심 결과, 다음 글의 주제, 도구 선택에 대한 판단, 아직 해결되지 않은 질문, 작성자의 짧은 감상 중 글에 맞는 방식을 선택한다.
- “다.. 알아둡시다”, “궁금한 건 물어봐주세요”처럼 짧게 끝낼 수도 있고, 해결되지 않은 문제를 제품이나 다음 연구 주제로 연결할 수도 있다. 매번 같은 결론 문장을 반복하지 않는다.

### 설명 방식

- 정의부터 나열하기보다 독자가 겪을 법한 구체적인 문제나 시스템 상황을 먼저 보여준다.
- 추상적인 개념은 1~26번 원문처럼 팀 프로젝트, PR 검토, 도서관에서 책 찾기, 호텔 카운터, 클라우드 서버, 웹 요청 등 익숙한 사례나 비유로 연결한다.
- 비유를 제시한 뒤 실제 기술적 의미와 비유의 한계도 설명한다.
- 독자가 가질 만한 후속 질문을 생략하지 않는다. “그렇다면”, “여기서 궁금한 점은”, “반대로”를 이용해 질문과 답이 이어지게 한다.
- 비슷해서 혼동하기 쉬운 개념은 짧은 문단, 목록, 나란한 예시를 이용해 비교한다. 표로 정리하지 않는다.
- 기술을 무조건 추천하지 않고, 쓰지 않아도 되는 조건과 운영상 비용까지 함께 제시한다.
- 중요한 주장은 가능한 한 공식 문서, 표준, 논문 등 1차 출처를 각주로 남긴다.

### 기본 구성

주제에 맞게 조절하되 대체로 다음 흐름을 따른다.

1. 독자가 겪을 법한 문제와 이번 글의 목표
2. 기존 방식이 부족한 이유
3. 핵심 개념과 용어를 쉬운 말로 설명
4. 구조 또는 내부 동작 원리
5. 직접 따라 할 수 있는 실습
6. 명령·코드의 이유와 실행 결과 해석
7. 실패 상황, 보안 문제, 예외 및 주의점
8. 실제 서비스에서의 선택 기준
9. 핵심 요약과 자연스러운 다음 글 예고

모든 글에 위 항목을 기계적으로 넣지는 않는다. 개념 글은 원리와 비교에, 실습 글은 재현 과정과 결과 해석에 더 큰 비중을 둔다.

### 실습과 코드

- 명령어만 나열하지 말고 “왜 실행하는가 → 코드/명령 → 예상 결과 → 결과가 뜻하는 것” 순으로 설명한다.
- 독자가 빈 환경에서도 따라 할 수 있도록 전제 조건, 디렉터리 구조, 의존성, 실행 순서를 빠뜨리지 않는다.
- 예제는 가능하면 하나의 현실적인 시나리오를 글 끝까지 유지한다.
- 출력 결과가 핵심 주장과 어떻게 연결되는지 반드시 해석한다.
- 정상 경로뿐 아니라 장애, 중복, 권한, 유실 등 실제 운영에서 문제가 되는 경계 조건도 다룬다.
- 위험한 보안 실습은 격리된 실습 환경을 전제로 하고, 악용보다 원리와 방어책에 중심을 둔다.
- 파일 전체나 독립적으로 저장할 코드를 제시할 때는 Expressive Code의 파일명 탭을 사용한다. 코드 블록의 여는 fence에서 언어 뒤에 `title="a.py"`를 적어 `python title="a.py"` 형태로 사용한다. `file_name=`, `filename=`, `name=`은 사용하지 않는다.
- 언어 식별자는 실제 코드에 맞춘다. 예: Python은 `python`, Docker Compose는 `yaml`, 셸 명령은 `bash`, 단순 출력은 `text`.
- 터미널에서 직접 실행하는 명령 묶음에는 파일명을 억지로 붙이지 않는다. 실제 파일 내용일 때만 `title="파일명"`을 사용한다.

### Markdown 표현

- `##` 소제목은 제목 앞에 랜덤한 이모지 하나를 붙인다. 이모지가 소제목 내용과 반드시 관련될 필요는 없으며, 에이전트가 의미에 맞는 이모지로 일관되게 정돈하지 않는다. 예: `## 🌽 Terraform의 작동 방식`, `## 🐤 Terraform의 새로운 규칙`.
- `###` 소제목은 이모지를 붙이지 않고 `1.`, `2.`, `3.`처럼 순서 번호를 붙이는 형식을 기본으로 사용한다. 예: `### 1. 기본 실습`, `### 2. 기존 방식의 문제점`.
- 같은 `##` 아래에 여러 `###`가 있으면 1부터 차례대로 번호를 매긴다. 새로운 `##`가 시작되면 `###` 번호도 다시 1부터 시작할 수 있다.
- 기존 글을 부분 수정할 때는 사용자가 요청하지 않은 소제목 형식까지 일괄 변경하지 않는다. 새 글을 쓰거나 소제목 구조를 새로 만드는 경우에 위 규칙을 적용한다.
- `[지난 글](URL)`처럼 Markdown 링크를 작성한 뒤 문장이 이어지면 닫는 괄호 다음을 반드시 한 칸 띄운다. 조사가 이어져도 `[지난 글](URL) 에서는`처럼 작성하며 `[지난 글](URL)에서는`처럼 붙이지 않는다.
- 포스팅 본문의 기술 용어, 라이브러리, 파일명, 설정값, 명령어를 인라인 백틱으로 감싸지 않는다. 예를 들어 \`confluent-kafka\`처럼 쓰지 않는다.
- 본문의 하이라이트는 목적에 따라 구분한다.
  - `==내용==`: 독자가 반드시 기억해야 할 핵심 개념이나 결론(노란색)
  - `++내용++`: 라이브러리·도구·파일명·객체명 등 글에서 지칭하는 대상(초록색)
  - `??내용??`: 명령어·옵션·설정 키와 값처럼 코드에 가까운 짧은 표현(파란색)
  - `!!내용!!`: 유실·보안 위험·금지 사항처럼 강한 경고가 필요한 표현(빨간색)
- 인라인 코드 표시는 사용하지 않되, 여러 줄 코드와 명령은 언어가 지정된 fenced code block으로 분리한다.
- `:::tip`, `:::warning`, `:::important` 등은 각각 팁, 실수하기 쉬운 지점, 핵심 원칙에 사용한다.
- **포스팅 본문에는 표를 작성하지 않는다.** 비교 항목이 많더라도 표로 요약하지 말고, 질문과 답이 이어지는 문단이나 목록으로 하나씩 풀어 설명한다.
- 기존 글을 수정할 때도 사용자가 명시적으로 요청하지 않는 한 새로운 표를 추가하거나 기존 설명을 표로 바꾸지 않는다.
- 강조 문법과 콜아웃을 문단마다 반복하지 않는다. 정말 기억해야 할 내용에만 사용한다.
- 마무리 형식은 글에 따라 달리한다. 필요한 경우에만 핵심 내용을 짧은 목록으로 정리하고, 요약 목록이 어색한 글은 실습 결과, 다음 질문, 작성자의 판단이나 다음 글 예고로 자연스럽게 끝낸다.

### 작성 전후 확인

- 새 포스팅의 날짜는 작업 당일이 아니라, 저장소에 존재하는 완성 포스팅 중 가장 최신 날짜의 다음 날짜로 지정한다. 디렉터리의 `YYYY-MM-DD`와 frontmatter의 `published` 날짜를 동일하게 맞춘다.
- 책을 바탕으로 작성하는 포스팅은 제목의 대괄호 `[]` 안에 카테고리명이 아니라 책 제목을 적는다. 예: `[AI 에이전트 엔지니어링] 독자의 질문 또는 글의 핵심 결과`.
- 책을 바탕으로 작성하는 포스팅의 `tags`에는 담당 범위가 속한 장을 `1장`, `2장`과 같은 형식으로 포함한다. 여러 장에 걸치면 해당 장을 각각 태그로 적는다.
- 작성 전 같은 시리즈의 직전 글을 읽고 이미 설명한 부분과 다음 글 예고를 확인한다.
- 문체를 확인할 목적으로 읽는 글은 반드시 postId 1~26에서 고른다. 27번 이후 글은 사실관계나 시리즈 연결을 확인할 필요가 있을 때만 자료로 읽을 수 있으며, 그 문체를 새 글에 반영하지 않는다.
- 기존 글을 읽었다고 가정하지 말고 필요한 개념은 짧게 복습하되, 같은 내용을 길게 반복하지 않는다.
- 제목은 `[카테고리] 독자의 질문 또는 글의 핵심 결과` 형태를 우선 고려한다.
- 초안 완성 후 사실관계, 명령어 재현성, 코드와 설명의 일치, 내부 링크, 출처를 확인한다.
- 명백한 오류가 아닌 문장은 에이전트의 취향으로 매끈하게 고쳐 쓰지 않는다. 작성자의 친근한 목소리와 다소 불규칙한 문장 호흡까지 보존한다.
- 초안에서 작성자 시점이나 직접 확인한 과정이 전혀 없다면 1~26번 글 특유의 설명자 목소리가 빠진 것은 아닌지 확인한다. 단, 모든 문단에 “제가”나 “저는”을 억지로 넣지는 않는다.
- 모든 문장이 비슷한 길이와 온도로 정돈되어 있다면 질문, 짧은 반응, 괄호 속 감상, 구체적인 경험을 넣을 지점을 검토한다.
- 여러 소제목이 같은 순서와 비슷한 분량으로 반복된다면, 표를 목록으로 바꾼 것처럼 구조만 남은 것은 아닌지 확인한다. 각 개념이 앞에서 생긴 질문 때문에 등장하도록 다시 연결한다.
- 사례가 여러 개 연속해서 빈틈없이 배치되어 있다면 중심 사례 하나만 자세히 남기고 나머지는 짧게 연결할 수 있는지 검토한다.
- 문단 끝마다 핵심을 한 문장으로 압축하거나 멋있게 완성된 교훈을 붙였다면 제거를 우선 검토한다. 작성자의 실제 판단에서 자연스럽게 나온 문장인지 확인한다.
- 글을 사람처럼 보이게 만들 목적으로 오타, 불확실한 표현, 구어체를 인위적으로 추가하지 않는다. AI 느낌은 문법의 정확성보다 지나치게 균일하고 대칭적인 구조에서 먼저 확인한다.
- 최종 검토에서는 27번 이후 글에서만 나타나는 표현이나 구성이 무의식적으로 섞이지 않았는지 확인한다.
